/**
 * Offline-safe font tokens.
 *
 * The original build fetched Google fonts at runtime, which makes local
 * startup fragile in restricted environments. These variables keep the theme
 * structure intact while using system font fallbacks.
 */
export const fontVariables = "";
